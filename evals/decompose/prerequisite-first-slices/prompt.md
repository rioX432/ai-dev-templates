---
name: decompose-prerequisite-first-slices
description: "Behavior slices with the shared prerequisite first, tests inside each slice, each with a Verify command"
tags: [decompose]
expected_outcome: "Behavior slices with the shared prerequisite first, tests inside each slice, each with a Verify command"
max_turns: 14
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill]
---

Decompose: add a favorites feature to a KMP app — a data model, a repository, a use case, a Compose screen, and tests.
